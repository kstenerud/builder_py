# Builder configuration file
# Specifies where to download the builder binary from

# URL pointing to a Git repository containing the rust project
builder_binary: "https://github.com/kstenerud/builder-test.git"
